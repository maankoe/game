import { expect } from "chai";
import { mockCall } from "../src/mocking";
import {
    // CanvasContext,
    // MockCanvasContext
} from "../src/canvas";


describe("MockCanvasContext", () => {
    it("Constructor", () => {
        expect(1).to.equal(2);
    });
});